$TIMESCALE_DB is an extension for PostgreSQL that enables time-series workloads, 
increasing ingest, query, storage and analytics performance. 

Best practice is to run $TIMESCALE_DB in a [$SERVICE_LONG](https://console.cloud.tigerdata.com/signup), but if you want to 
self-host you can run $TIMESCALE_DB yourself.

