<Highlight type="note">

You might also notice that the metadata fields are missing. Because this is a
relational database, metadata can be stored in a secondary table and `JOIN`ed at
query time. Learn more about [$TIMESCALE_DB's support for `JOIN`s][joins-with-relational-data-link].

</Highlight>

[joins-with-relational-data-link]: /_partials/:currentVersion:/_data_model_metadata/#joins-with-relational-data
